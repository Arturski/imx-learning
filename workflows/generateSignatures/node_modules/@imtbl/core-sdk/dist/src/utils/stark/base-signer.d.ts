import { ec } from 'elliptic';
import { L2Signer } from '../../types';
export declare class BaseSigner implements L2Signer {
    private keyPair;
    constructor(keyPair: ec.KeyPair);
    getAddress(): string;
    signMessage(msg: string): Promise<string>;
    private fixMessage;
    private serialize;
}
