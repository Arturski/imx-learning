import { TradesApi, GetSignableTradeRequest, CreateTradeResponse } from '../api';
import { WalletConnection } from '../types';
declare type createTradeWorkflowParams = WalletConnection & {
    request: GetSignableTradeRequest;
    tradesApi: TradesApi;
};
export declare function createTradeWorkflow({ l1Signer, l2Signer, request, tradesApi, }: createTradeWorkflowParams): Promise<CreateTradeResponse>;
export {};
